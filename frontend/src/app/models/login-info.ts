/**
 * This is an export class of Login info
 */
export class LoginInfo {
  /**
   * it is a variable of type string that caries user email
   */
  email: string;
  /**
   * it is a variable of type string that caries user password
   */
  password: string;
  /**
   * it is a variable of type string
   */
  confirmPassword: string;
  /**
   * it is a variable of type string that caries user first name
   */
  firstName: string;
  /**
   * this variable carries the full name of the user
   */
  fullName: string;
  /**
   * it is a variable of type string that caries user last name
   */
  lastName: string;
  /**
   * this variable carries the id of the user
   */
  userId: number;
  /**
   * this variable carries the id of the user
   */
  id: number;
  /**
   * it is a variable of type string
   */
  role: string;
  /**
   * it is a variable of type string
   */
  userType: string;
  /**
   * it is a variable of type string
   */
  token: string;
  /**
   * this variable carries the ip of the user
   */
  ip: string;
}
